--- a/rtl_433.c
+++ b/rtl_433.c
@@ -1,6 +1,7 @@
 void iq_proc(r_cfg_t *cfg,  struct dm_state *demod)
 {
 
     uint32_t sample_rate_0 = cfg->samp_rate;
     zmq_config* zmq_info = cfg->zmq_info;
     zmq_start(zmq_info);
+    print_logf(LOG_NOTICE, "ZMQ", "Starting IQ processing (cfg->samp_rate=%u)", cfg->samp_rate);
 
     // Special case for in files or zmq
     if (cfg->in_files.len) {
-        unsigned char *test_mode_buf = malloc(DEFAULT_BUF_LENGTH * sizeof(unsigned char));
+        /* test_mode_buf holds raw bytes that will be passed to sdr_callback */
+        unsigned char *test_mode_buf = malloc(DEFAULT_BUF_LENGTH);
         if (!test_mode_buf)
             FATAL_MALLOC("test_mode_buf");
-        float *test_mode_float_buf = malloc(DEFAULT_BUF_LENGTH / sizeof(int16_t) * sizeof(float));
+
+        /* For CF32 -> int16 conversion we need one float per int16 sample.
+         * DEFAULT_BUF_LENGTH is bytes for the final buffer (e.g. bytes for int16 samples).
+         * int16_count = number of int16 samples we will produce.
+         */
+        size_t int16_count = DEFAULT_BUF_LENGTH / sizeof(int16_t);
+        float *test_mode_float_buf = malloc(int16_count * sizeof(float));
         if (!test_mode_float_buf)
             FATAL_MALLOC("test_mode_float_buf");
 
@@ -40,57 +47,39 @@ void iq_proc(r_cfg_t *cfg,  struct dm_state *demod)
             FILE *fp;
             fp = fopen("TEST.iq", "w+");
 
             // default case for file-inputs
             int n_blocks = 0;
             unsigned long n_read;
             delay_timer_t delay_timer;
             delay_timer_init(&delay_timer);
             do {
                 // Replay in realtime if requested
                 if (cfg->in_replay) {
                     // per block delay
                     unsigned delay_us = (unsigned)(1000000llu * DEFAULT_BUF_LENGTH / cfg->samp_rate / demod->sample_size / cfg->in_replay);
                     if (demod->load_info.format == CF32_IQ)
                         delay_us /= 2; // adjust for float only reading half as many samples
                     delay_timer_wait(&delay_timer, delay_us);
                 }
                 // HERE
                 // Convert CF32 file to CS16 buffer
 
 
                 if (demod->load_info.format == CF32_IQ) {
-                    //n_read = fread(test_mode_float_buf, sizeof(float), DEFAULT_BUF_LENGTH / 2, in_file);
-                    float tmp[16384*2];
-                    
-                    size_t last_fill_point = 0;  // where we left off filling test_mode_float_buf
-                    size_t iloc = 0;
-                    unsigned int num_points = 0;
-                    unsigned int load_size = DEFAULT_BUF_LENGTH / 2;
-                    while(1)
-                    {
-                        iloc = 0; 
-                        n_read = zmq_recv(zmq_info->requester, tmp, load_size, 0);
-                        //printf("n read: %li\n", n_read);
-                        num_points = n_read / sizeof(float);
-                        while((last_fill_point < DEFAULT_BUF_LENGTH / 2) && (iloc < num_points))
-                        {
-                            test_mode_float_buf[last_fill_point] = tmp[iloc];
-                            //fprintf(fp, "%f\n", tmp[iloc]);
-                            last_fill_point++;
-                            iloc++;
-                        }
-
-                        if(last_fill_point == DEFAULT_BUF_LENGTH/2)
-                        {
-                            //printf("All full: %i\n", last_fill_point);
-                            last_fill_point = 0;
-                            iloc = 0;
-                            load_size = DEFAULT_BUF_LENGTH / 2;
-                            break;
-                        }
-
-                        load_size = load_size - num_points;
-                        //sleep(1);
-                    }
-
-                    
-                    for(int i = 0; i < DEFAULT_BUF_LENGTH / 2; i++)
-                    {
-                        if(test_mode_float_buf[i] == 0)
-                        {
-                            printf("Zero: %i\n", i);
-                            break; 
-                        }
-                    }
-                    
-                    n_read = DEFAULT_BUF_LENGTH / 2;
-                    // clamp float to [-1,1] and scale to Q0.15
-                    for (unsigned long n = 0; n < n_read; n++) {
-                        int s_tmp = test_mode_float_buf[n] * INT16_MAX;
+                    /* CF32 path: read floats_needed floats (bytes_needed bytes) into test_mode_float_buf.
+                     * We will then convert floats -> int16 in test_mode_buf.
+                     */
+                    size_t floats_needed = int16_count; /* one float -> one int16 sample */
+                    size_t bytes_needed = floats_needed * sizeof(float);
+                    size_t bytes_filled = 0;
+                    while (bytes_filled < bytes_needed) {
+                        ssize_t r = zmq_recv(zmq_info->requester,
+                                             ((uint8_t*)test_mode_float_buf) + bytes_filled,
+                                             bytes_needed - bytes_filled,
+                                             0);
+                        if (r <= 0) {
+                            if (r == 0) {
+                                /* No data; break out and handle as EOF/short read */
+                                print_logf(LOG_WARNING, "ZMQ", "zmq_recv returned 0 while reading CF32 data");
+                            } else {
+                                perror("zmq_recv");
+                                print_logf(LOG_ERROR, "ZMQ", "zmq_recv error while reading CF32");
+                            }
+                            break;
+                        }
+                        bytes_filled += (size_t) r;
+                    }
+
+                    size_t floats_read = bytes_filled / sizeof(float);
+                    if (floats_read == 0) {
+                        n_read = 0;
+                    } else {
+                        /* Convert floats to int16 in-place into test_mode_buf */
+                        for (size_t i = 0; i < floats_read; ++i) {
+                            int tmp = (int)(test_mode_float_buf[i] * INT16_MAX);
+                            if (tmp < -INT16_MAX) tmp = -INT16_MAX;
+                            else if (tmp > INT16_MAX) tmp = INT16_MAX;
+                            ((int16_t *)test_mode_buf)[i] = (int16_t) tmp;
+                        }
+                        n_read = floats_read * sizeof(int16_t); /* bytes ready for sdr_callback */
+                    }
                 } else {
+                    /* Non-float path: read directly into test_mode_buf until DEFAULT_BUF_LENGTH bytes */
+                    size_t bytes_needed = DEFAULT_BUF_LENGTH;
+                    size_t bytes_filled = 0;
+                    while (bytes_filled < bytes_needed) {
+                        ssize_t r = zmq_recv(zmq_info->requester,
+                                             test_mode_buf + bytes_filled,
+                                             bytes_needed - bytes_filled,
+                                             0);
+                        if (r <= 0) {
+                            if (r == 0) {
+                                print_logf(LOG_WARNING, "ZMQ", "zmq_recv returned 0 while reading bytes");
+                            } else {
+                                perror("zmq_recv");
+                                print_logf(LOG_ERROR, "ZMQ", "zmq_recv error while reading bytes");
+                            }
+                            break;
+                        }
+                        bytes_filled += (size_t)r;
+                    }
+                    n_read = bytes_filled;
+
+                    /* Convert CS8 (signed int8) to CU8 (unsigned) if needed */
                     if (demod->load_info.format == CS8_IQ) {
-                        for (unsigned long n = 0; n < n_read; n++) {
+                        for (size_t i = 0; i < n_read; ++i) {
-                            test_mode_buf[n] = ((int8_t)test_mode_buf[n]) + 128;
+                            test_mode_buf[i] = ((int8_t)test_mode_buf[i]) + 128;
                         }
                     }
                 }
                 if (n_read == 0) break;  // sdr_callback() will Segmentation Fault with len=0
                 demod->sample_file_pos = ((float)n_blocks * DEFAULT_BUF_LENGTH + n_read) / cfg->samp_rate / demod->sample_size;
                 n_blocks++; // this assumes n_read == DEFAULT_BUF_LENGTH
-                //printf("Callback\n");
+                print_logf(LOG_DEBUG, "ZMQ", "Block %d: bytes_in=%lu samples_out=%lu cfg->samp_rate=%u",
+                           n_blocks, (unsigned long)n_read, (unsigned long)(n_read / demod->sample_size), cfg->samp_rate);
                 sdr_callback(test_mode_buf, n_read, cfg);
             } while (n_read != 0 && !cfg->exit_async);
             fclose(fp);
